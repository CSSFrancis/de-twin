Example gallery
===============

Images, diffraction patterns and detector frames rendered by the twin. Every example
runs in-process on the small DESim sensor with a ``ManualClock``, so it is deterministic
and needs nothing else running.
